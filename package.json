{
  "name": "andergo-web",
  "version": "1.0.0",
  "private": true,
  "description": "Supabase-connected backend for the ANDERGO landing page prototype",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "npm run build && node lib/server.js",
    "dev": "npm run build && node lib/server.js",
    "sync:worlds": "node scripts/sync-worlds-from-seed.js",
    "build": "node scripts/build-static.js",
    "test": "node --test server.test.js",
    "db:setup": "node scripts/setup-database.js",
    "audio:generate": "node scripts/generate-listening-audio.js",
    "verify:all": "node scripts/verify-all.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.0",
    "dotenv": "^16.4.1",
    "express": "^4.19.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "openai": "^6.46.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "prettier": "^3.9.5"
  }
}
